<h1>Dev/Local Jobs</h1>

<nav> 
  I'll be a search box someday! 
  <button>
    <%= link_to "List your job", new_listing_path %>
  </button>
</nav>

<ul>
  <% @listings.each do |l| %>
    <li>
      <%= link_to l.title, listing_path(l.id) %>, <%= l.headquarters %>
    </li>
  <% end %>
</ul>

