<%= render partial: "shared/header_small" %>
<h1>Pending Listings</h1>

<ul>
<% @listings.pending.each do |l| %>

  <li>
   <%= link_to l.title, listing_review_path(l.id) %>, <%= l.created_at.to_date %>, <%= l.approved %>
 </li>

<% end %>
</ul>

<p>See Rejected Listings</p>